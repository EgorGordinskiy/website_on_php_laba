<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
  </head>
  <body>
    <header class="header">
      <div class="conteiner">
        <div class="header__inner">
          <a href="/" class="header__logo"
            ><img class="header__logo-img" src="/images/logo.svg"
          /></a>
          <nav class="header__nav">
            <ul class="header__list">
              {% for type in types %}
              <li class="header__item">
                <a
                  class="header__link {{ title == type.type ? 'active' }}"
                  href="/?type={{ type.type }}"
                  >{{ type.type | title }}</a
                >
              </li>
              {% endfor %}
              <li class="header__item">
                <a
                  class="header__link {{ title == type.type ? 'active' }}"
                  href="/search"
                  >Поиск</a
                >
              </li>
              <li class="header__item">
                <a
                  class="header__link {{ title == type.type ? 'active' }}"
                  href="/create"
                  >Добавить персонажа</a
                >
              </li>
              <li class="header__item">
                <a
                  class="header__link {{ title == type.type ? 'active' }}"
                  href="/type/create"
                  >Добавить семью</a
                >
              </li>
            </ul>
          </nav>
        </div>
      </div>
    </header>
    <main>
      <section>
        <div class="conteiner">{% block content %} тьма... {% endblock %}</div>
      </section>
    </main>
  </body>
</html>
