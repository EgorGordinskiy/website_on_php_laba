<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
  </head>
  <body>
    <header class="header">
      <div class="conteiner">
        <div class="header__inner">
          <a href="#" class="header__logo"
            ><img class="header__logo-img" src="/images/logo.svg"
          /></a>
          <nav class="header__nav">
            <ul class="header__list">
              {% for item in menu %}
              <li class="header__item">
                <a
                  class="header__link {{ title == item.title ? 'active' }}"
                  href="{{ item.url }}"
                  >{{ item.title }}</a
                >
              </li>
              {% endfor %}
            </ul>
          </nav>
        </div>
      </div>
    </header>
    <main>
      <section>
        <div class="conteiner">{% block content %} пустота {% endblock %}</div>
      </section>
    </main>
  </body>
</html>
